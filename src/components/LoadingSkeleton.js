import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LoadingSkeleton = () => {
  return (
    <View>
      <Text>LoadingSkeleton</Text>
    </View>
  );
};

export default LoadingSkeleton;

const styles = StyleSheet.create({});
